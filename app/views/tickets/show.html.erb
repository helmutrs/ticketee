<div id="ticket">
  <h2><%= @ticket.title %></h2>
  <%= simple_format(@ticket.description) %>
  <%= link_to "Edit Ticket", edit_project_ticket_path %> <!-- or [:edit, @project, @ticket] this is the array which means
   since we're on the show page for the tickets, means edit the tickets for this project -->
  <%= link_to "Delete Ticket", [@project, @ticket], :method => :delete, :confirm => "Are you sure you want to delete this ticket?" %>
</div>